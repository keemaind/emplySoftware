<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!--#region ContactsListViewItem-->
    <Style x:Key="ContactsListViewItem" TargetType="ListViewItem">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="{StaticResource MC01}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">

                    <Border Height="50"
                            Margin="10,5"
                            Padding="10"
                            x:Name="border"
                            CornerRadius="5">

                        <StackPanel Orientation="Horizontal">

                            <Ellipse x:Name="ProfilePhotoChats"
                                     Width="30" Height="30"
                                     Margin="0,0,10,0">

                                <Ellipse.Fill>

                                    <ImageBrush x:Name="UserChatsImages"
                                                ImageSource="{Binding Image}"
                                                Stretch="UniformToFill"
                                                RenderOptions.BitmapScalingMode="HighQuality"/>

                                </Ellipse.Fill>

                            </Ellipse>

                            <StackPanel>

                                <TextBlock  Style="{StaticResource ProfileNamesTextBlock}"
                                        Text="{Binding Title}"/>

                                <TextBlock  Style="{StaticResource ProfileJobTextBlock}"
                                        Text="This is just simple text for testing, don't worry."/>

                            </StackPanel>

                        </StackPanel>

                        <Border.Background>

                            <SolidColorBrush Color="#FFF" x:Name="BorderBackground"/>

                        </Border.Background>

                    </Border>

                    <ControlTemplate.Triggers>

                        <EventTrigger RoutedEvent="Border.MouseEnter" SourceName="border">
                            <BeginStoryboard>

                                <Storyboard>

                                    <ColorAnimation Storyboard.TargetName="BorderBackground"
                                                    Storyboard.TargetProperty="Color"
                                                    From="#FFFFFF"
                                                    To="#F7F8FA"
                                                    Duration="0:0:0:0.2"/>
                                    
                                </Storyboard>

                            </BeginStoryboard>

                        </EventTrigger>

                        <EventTrigger RoutedEvent="Border.MouseLeave" SourceName="border">

                            <BeginStoryboard>

                                <Storyboard>

                                    <ColorAnimation Storyboard.TargetName="BorderBackground"
                                                    Storyboard.TargetProperty="Color"
                                                    From="#F7F8FA"
                                                    To="#FFFFFF"
                                                    Duration="0:0:0:0.2"/>
                                    
                                </Storyboard>

                            </BeginStoryboard>

                        </EventTrigger>

                        <Trigger Property="IsSelected" Value="True">

                            <Setter Property="Background" Value="{StaticResource MC04}" TargetName="border"/>

                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
                
            </Setter.Value>
            
        </Setter>
            
    </Style>
    <!--#endregion-->
    
</ResourceDictionary>